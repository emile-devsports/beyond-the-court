# Beyond the Court — report (interim static host)

Single-page static site hosting the Churchill Fellowship report
*Beyond the Court: Late-Night Basketball for Social Cohesion and Change*.

- `index.html` — the full report (self-contained except Google Fonts).
- `CNAME` — custom domain for GitHub Pages (`beyondthecourtproject.com`).

Interim host ahead of the full site build. Deployed via GitHub Pages.
